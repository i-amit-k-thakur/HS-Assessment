HS-Assesment


HOW TO USE:
- Created an iOS application.
- The GET request is done as soon as the view is loaded.
- Once the data is received correctly, the app displays the 'Validate Data' button
- Tapping the button validates the data and calls the POST endpoint.
- The app displays an alert to the user according to the response.
